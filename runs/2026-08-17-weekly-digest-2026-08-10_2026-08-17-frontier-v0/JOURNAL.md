## 2026-08-17 Lane B (writing)

Same sweep, 77 items land in W2.

Leads worth adjudicating against the primary record before anything is claimed:

- Flue, already on the watchlist at Tier 2, is written up as bringing React-style
  hooks to a meta-harness (latent-space, 08-15). A profile-bearing claim if the
  primary record supports it.
- DeepSeek V4 Pro 0813 appears on OpenRouter (simonw, 08-12), one day before the
  Harness repo went public. The pairing of a model release with a harness release
  is the thing this publication watches for.
- Cloudflare on detecting and securing MCP traffic (08-14).
- Martin Fowler on whether TDD inside the agent loop is theatre or value (08-11).
  Not a product change; possible wire item.
