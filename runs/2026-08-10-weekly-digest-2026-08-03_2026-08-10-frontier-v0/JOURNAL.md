## 2026-08-17 Lane B (writing)

One roster sweep since 2026-08-03, 15/15 feeds live, 119 items, split by item
date: 42 land in W1. No feed failed, so there is no gap to record this window.

Leads worth adjudicating against the primary record before anything is claimed:

- Claude Code auto mode reported as the new default on Pro, Max and Team plans
  (simonw, 08-08). If that holds up it is a permission-posture change, not a
  feature note, and it also bears on the prior window's carry-forward question
  about whether the publish cadence resumed.
- Cloudflare ran an Agents Week across 08-04..08-06: an agent access model, an
  agent-first browser running in V8 isolates, WebMCP for arbitrary sites, and a
  "next generation of MCP" post. Adjacent to the watchlist rather than on it,
  but it is the largest single-vendor move in the window.
- embrace-the-red published an attack on LiteLLM (08-03) covering traffic
  interception, key theft and tool-call injection. This is the lane's whole
  reason for existing: a security writeup on a blog that an X sweep can miss.
