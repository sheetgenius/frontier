# Harvest: Flue (Tier 2; window 2026-06-04 .. 2026-06-16)

coverage: tool_access_ok=yes. Surfaces: github.com/withastro/flue/blob/main/CHANGELOG.md (canonical receipt — Flue ships NO GitHub Releases), raw.githubusercontent CHANGELOG, commits/main/CHANGELOG.md, npm registry @flue/sdk + @flue/cli (authoritative publish dates). The bare `flue` npm package is a dead 2015 squat (0.2.6) — real packages are @flue/sdk + @flue/cli. nothing_in_window=false: SIX in-window versions (0.10.0, 0.10.1, 0.10.2, 0.11.0, 0.11.1, 1.0.0-beta.1). Dating triangulated CHANGELOG date vs npm publish UTC vs release commit; 0.10.2 + 0.11.0 CHANGELOG say 06-09 but npm publish 06-10 00:07Z / 05:22Z (off-by-one local tz) — both in-window. No 0.9.3 (0.9.x ended at 0.9.2, out-of-window baseline). YEAR=2026 confirmed via npm ISO timestamps.

## Findings

1. **2026-06-08-flue-v0100-durable-agent-execution** (2026-06-08; 0.10.0+0.10.1) — breaking/runtime. "Unified durable agent execution" + "Pluggable persistence" (sqlite() and @flue/postgres) + "Signal messages and mid-turn recovery" + "Cloudflare extension hooks". 5 breaking changes incl. **"Cloudflare durable deployments require a migration"**, "Persistence adapter contracts changed", removed tool_execution_* events. 0.10.1 bumped engine @earendil-works/pi-ai + pi-agent-core to 0.79.0. The ephemeral→durable/recoverable inflection. security=none. Receipt: CHANGELOG.md — "## 0.10.0 … Unified durable agent execution / Pluggable persistence / Signal messages and mid-turn recovery / Cloudflare durable deployments require a migration."

2. **2026-06-10-flue-v0102-durable-streams-replace-websocket-sse** (2026-06-10; 0.10.2) — **breaking/protocol**. **"Durable Streams protocol replaces WebSocket and SSE transports."** + "Named sessions removed from agent public API" + SDK rewritten with @durable-streams/client + connectEventStreamStore() now required on PersistenceAdapter. Abandons two standard transports for a proprietary protocol ONE CYCLE after WS credential hardening. Answers "is run history externally consumable?" in the CLOSED direction. accessibility=negative (integrators must rewrite to DS client). security=none. Receipt: CHANGELOG.md — "## 0.10.2 … Durable Streams protocol replaces WebSocket and SSE transports. / Named sessions removed from agent public API. / connectEventStreamStore() is now required on PersistenceAdapter."

3. **2026-06-10-flue-v0110-offline-docs-and-image-byte-redaction** (2026-06-10; 0.11.0) — capability/security. "flue docs browses the documentation offline." + **"Runtime events no longer carry raw image bytes"** (image data→IMAGE_DATA_OMITTED sentinel, mimeType retained) — payload/observability hygiene; operators persisting pre-0.11.0 history may hold raw image payloads. security: hygiene (no formal advisory). Receipt: CHANGELOG.md — "## 0.11.0 … flue docs browses the documentation offline. / Runtime events no longer carry raw image bytes."

4. **2026-06-11-flue-v0111-image-inputs-and-ripgrep** (2026-06-11; 0.11.1) — capability. **Multimodal image input**: "agent HTTP requests and @flue/sdk prompts can include images with up to 14 MiB encoded data per image" (chunked SQLite storage); grep tool uses ripgrep when available; per-row session history. security=none. Receipt: CHANGELOG.md — "## 0.11.1 … Direct agent HTTP requests and @flue/sdk prompts can include images with up 14 MiB of encoded data per image."

5. **2026-06-16-flue-v100-beta1-leading-release** (2026-06-16; 1.0.0-beta.1) — **breaking/ecosystem/capability**. The **leading 1.0 beta** — API-stability intent (experimental caveat starting to lift). ~11 breaking: persistence adapters→single async connect(); **run IDs now opaque run_<ulid>**; **defineTool parameters TypeBox→valibot**; cloudflareSandbox() replaces workerd stub; **observe() now receives every event**; provider-native webhook payloads (GitHub/Slack/Discord/Google Chat). New: @flue/runtime exports **listRuns()/getRun()/listAgents()** (run-introspection — partially reopens observability door 0.10.2 closed); **@flue/react** (FlueProvider/useFlueAgent/useFlueWorkflow); first-party verified-HTTP-ingress integration pkgs (@flue/stripe/notion/resend); driver-free @flue/mysql/redis/mongodb; `flue add <kind> <name|url>` blueprints; npm-importable skills (also commit cfc94c1, 06-14). Migration-heavy. security: capability framing (verified ingress = first-party signature verification). Receipt: CHANGELOG.md — "## 1.0.0-beta.1 … Run IDs are now opaque run_<ulid> values / defineTool({ parameters }) now uses valibot instead of TypeBox / observe() now receives every event directly / @flue/runtime exports listRuns(), getRun(), and listAgents()."

## Top signals (harvester's pick)
1. v100-beta1 — leading 1.0 of a model+harness framework; experimental caveat lifting; migration-heavy; listRuns/getRun/listAgents = receipt/observability precedent.
2. v0102-durable-streams — swapped standard WS+SSE for proprietary Durable Streams; cautionary precedent on coupling observability to non-standard transport.
3. v0100-durable-agent-execution — ephemeral→durable recoverable execution w/ pluggable persistence; harness maturing into stateful infra; @earendil-works/pi-* engine visible underneath.
(Lower: v0111 multimodal input; v0110 offline docs + image-byte hygiene.)
