# Harvest: Hermes Agent (window 2026-06-04 .. 2026-06-16)

coverage: tool_access_ok=yes (GitHub web + REST/gh api; docs site reachable but version-silent). One tagged release v0.16.0 (v2026.6.5, 2026-06-06T00:55Z) + dense post-release commit stream 06-07→06-16. NOTE: history rewritten in-window (backup tags premerge-oh-god etc); many commits author-dated weeks earlier than committer-date — dated by COMMITTER date (actual landing). 06-04/06-05 empty in committer stream (absorbed under v0.16.0 tag). v0.15.x OOW (dedupe). Unauth API rate-limited mid-harvest → switched to authenticated gh api; no fabrication.

## Findings (5)

1. **2026-06-06-hermes-agent-v0.16.0-surface-release** (06-06; v0.16.0/v2026.6.5) — **accessibility/capability**. "The Surface Release": native **Electron desktop app** (mac/Linux/Win, one-click install, self-update, drag-drop, inline model picker); **remote-gateway connect** from desktop over OAuth OR username/password, multi-profile sessions, cross-profile @session links; **browser web-admin dashboard** (MCP toggles, channels, credentials, webhooks, memory, gateway controls, pluggable OIDC/user-pass login); Quick Setup via Nous Portal; /undo [N] turn rollback. 874 commits/542 PRs/399 issues (2 P0, 62 P1, **16 security-tagged**). Bundled: CVE-2026-48710 (Starlette BadHost) pinned ≥1.0.1; SSRF off event loop; Bedrock tokens stripped from subprocess env; Docker restart/stop/kill → DANGEROUS_PATTERNS. NEW authority boundary = dashboard auth gate. accessibility=large (install-to-first-message in seconds; GUI for non-terminal users). Receipt: release v2026.6.5.

2. **2026-06-13-hermes-agent-sensitive-write-and-status-leak-hardening** (06-13 + 06-07 sibling; post-release main, untagged) — **security (highest governance)**. Fail-closed wave fixing Hermes's OWN guardrail theater: (a) approval.py now denies cp|mv|install into sensitive paths (~/.ssh/*, ~/.netrc/.pgpass/.npmrc, shell rc, ~/.hermes/config) — closed an auto-approve gap where `cp evil ~/.ssh/authorized_keys` (SSH-key implant) passed while tee/`>` forms were denied ("an unpaired write deny is theater"); (b) unauth /api/status stopped leaking hermes_home/config_path/env_path/gateway PID/health URL on gated binds; (c) own-policy adapters (WhatsApp/WeCom/Weixin/QQBot/Yuanbao) FAIL CLOSED when enabled with creds but no allowlist (was fail-OPEN, violating their own SECURITY.md §2.6); (d 06-07) command-denylist normalizer strips backslash/empty-quote bypasses, tui_gateway shell.exec fails closed if tools.approval unimportable. **NOT in v0.16.0 release** — exposed binary users lack these. Receipt: commits da28d5d11/3380563d9/fc4635458/621bf3a87.

3. **2026-06-15-hermes-agent-async-background-subagents** (06-15; post-release main, untagged) — **capability (multi-agent)**. `delegate_task(background=true)` (#40946): dispatches async subagent, returns handle immediately; full result + original task re-enters conversation as NEW turn on completion-queue rail. async_delegation.py registry + capacity cap; config delegation.max_async_children (default 3); detach-at-dispatch (survive parent interrupt), atomic capacity (TOCTOU fix); /stop + /agents control surface. Sibling bba9b519a (06-12): removed default 600s subagent wall-clock timeout (DEFAULT_CHILD_TIMEOUT 600→None) — runaway detection now leans on heartbeat-staleness only. Changes unit-of-work + receipt boundary. Receipt: commit c66ecf0bc.

4. **2026-06-16-hermes-agent-skill-memory-poisoning-fixes** (06-16; post-release main, untagged) — **security (self-improving-agent risk class)**. (a) Skill-memory poisoning: every auto-syncing memory provider (mem0, hindsight, retaindb, byterover, honcho, supermemory, openviking) stored/embedded the RAW user turn → a /skill invocation poisoned the store with the full skill body; fix strips skill scaffolding once in MemoryManager fan-out. (b) Recursive skill-delete tree-escape: skill_manage(action='delete') did bare shutil.rmtree with no validation (ported from Kilo-Org incident that "lost a user's entire working directory"); adds _validate_delete_target() refusing rmtree outside skills root / on root / via symlink. Receipt: commits c2c55c444/e3adbb5ae/2dbc3bd93.

5. **2026-06-14-hermes-agent-credential-and-ssl-resilience** (06-14..06-16; post-release main, untagged) — reliability/credential. SSL CA-bundle fail-fast guard (ssl_guard.py, doctor section, HERMES_SKIP_SSL_GUARD escape) — stale certifi after partial `hermes update` no longer crashes agent/traps gateway in retry loop; credential siblings: self-heal Codex token reimport from ~/.codex, xAI OAuth cross-profile resolution + earlier refresh, classify exhausted pool-only Codex cred as rate-limited not missing-auth, **redact secrets in API debug dumps** (ad58dd51a), guard OAuth account removal, fail-fast non-interactive MCP OAuth without tokens. Receipt: commits a218a0f15/ad58dd51a/9a59ad73d.

## Top signals (harvester's pick)
1. v0.16.0-surface-release — desktop GUI + browser admin + remote-gateway connect (OAuth/user-pass); changes who can drive an agent + new authority boundary. Only tagged release; load-bearing.
2. sensitive-write-and-status-leak-hardening — fail-closed wave fixing Hermes's OWN documented-control-vs-actual-enforcement gaps (cp→.ssh auto-approve, status leak, fail-open adapters). UNSHIPPED (main, post-release) — v0.16.0 binary still exposed.
3. async-background-subagents — fire-and-forget delegate_task(background=true) re-injecting results as new turns + removed default timeout. Multi-agent orchestration delta.
4. skill-memory-poisoning-fixes — skills poisoning every memory provider + agent-triggered recursive delete wiping cwd. Self-improving-agent risk class.
5. credential-and-ssl-resilience — provider-router/credential-pool hardening + debug-dump secret redaction.
